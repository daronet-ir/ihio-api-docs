type: object
required:
  - cpartySessionId
  - citizenSessionId
  - printCode
properties:
  cpartySessionId:
    type: string
    description: Session ID from authentication
  citizenSessionId:
    type: string
    description: Citizen session ID
  printCode:
    type: string
    description: Print code to be used for retrieving the prescription reference 