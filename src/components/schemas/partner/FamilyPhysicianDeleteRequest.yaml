type: object
description: Request to delete a family physician assignment for a citizen.
required:
  - cpartySessionId
  - citizenSessionId
properties:
  cpartySessionId:
    type: string
    description: Session ID from authentication of the calling party.
  citizenSessionId:
    type: string
    description: Session ID of the citizen/patient whose family physician assignment is to be deleted. 